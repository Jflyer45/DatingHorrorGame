tree("root")
	fallback
		while not CanSeeOrChasing
			tree("Patrol")
		tree("ChasePlayer")

tree("Patrol")
	sequence
		MoveTo("CloseRightCorner")
		Wait 0.5
		MoveTo("FarRightCorner")
		Wait 0.5
		MoveTo("FarLeftCorner")
		Wait 0.5
		MoveTo("CloseLeftCorner")
		Succeed

tree("ChasePlayer")
	sequence
		ChasePlayer()
